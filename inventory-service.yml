spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/application-database
    currentSchema: inventory_service
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: validate